import React from 'react';
import { Text, View } from 'react-native';

import styles from '../styles/style';

const Pause = () => (
  <View style={styles.pauseContainer}>
    <Text>paused game</Text>
  </View>
);

export default Pause;
